Days Between Transactions = 
DATEDIFF(
    MIN('FactSales'[SalesDate].[Day]), 
    MAX('FactSales'[SalesDate].[Day]), 
    DAY
) + 1
Sales Status = 
IF(
    'FactSales'[SalesAmount] > 'FactSales'[TargetSales], 
    "High", 
    "Low"
)
Total Sales Revenue = SUM('FactSales'[SalesAmount])
Total Unique Customers = DISTINCTCOUNT('FactSales'[CustomerID])

SUM(FactSales[SalesAmount])
AVERAGE(FactSales[SalesAmount])
MIN(FactSales[SalesAmount])
MAX(FactSales[SalesAmount])
COUNT(FactSales[OrderID])
COUNTROWS(FactSales)
DISTINCTCOUNT(FactSales[CustomerID])
Year = YEAR(DimDate[Date])
Total Sales → Measure name
SUM() → Function
Sales[SalesAmount] → Column reference

The total sales revenue achieved during the period was RM XXX, indicating the overall business performance.
Sales revenue showed an increasing trend over time, indicating positive business growth.
The highest sales revenue was generated by the ______ category.
The Electronics category generated the highest sales revenue compared to other product categories.
The total sales revenue achieved during the period was RM1000000

Sales revenue increased in several month, showing positive sales growth trends.

The Electonics category generated the highest sales revenue compared to other product categories
